# Changelog

## v6.3.1 — 结构重构（无功能变更）

纯文件组织优化，不修改任何审查规则。

- SKILL.md 1,649行 → ~487行（87KB → ~22KB）
- 新增 `references/`（11个文件），按阶段拆分为独立可加载模块
- 阶段索引升级为 🎯/⏭️/📄/📦 四字段可执行触发器
- 每个 reference 文件头部增加前置依赖声明
- Fast Path 拆分为概念层（SKILL.md）+ 规则表（p1-change-scan.md）
- Glossary、去重机制、暂存区规范、非文本过滤保留在 SKILL.md（跨阶段通用）

---

## v6.3.0 — 审计驱动修复：可信度与覆盖率

### 一句话

深度审计后集中修复 9 项：3 个 Critical 安全修复 + P0.5/P9 可信度增强 + Diff-Only 体验优化 + 文档补全。

### 修复清单

#### 🔴 Critical 修复（3项）

- **C1 版本号统一**：SKILL.md frontmatter `version: 1.0.0` → `6.3.0`，与 CHANGELOG 对齐
- **C2 LICENSE 创建**：添加 MIT LICENSE 文件，消除法律风险
- **C3 P2.4 依赖扫描描述修正**：明确两步流（全量 `npm audit` → 后处理按 diff 包名过滤），修复"仅扫新增包"的文档与实现矛盾

#### 🔴 可信度增强（2项）

- **P9 AI 结论降权**：AI 单独命中的结论来源标签改为 `🤖【AI推断-待确认】`，与静态规则 `【静态规则】` 视觉区分；静态规则+AI 同时命中时来源合并为 `【静态规则+AI评审】` 不加 🤖（有静态规则佐证）。资损升级通道门槛从 0.9 → 0.95，收紧 UX 衍生的 Critical 阻断
- **P0.5 Medium 场景透明化**：Medium 置信度场景在 P10 报表维度列加 `~` 前缀（如 `~资源抢占 P5.3`），表头注释 `~ 标记为P0.5推断，建议核实`，解决 Medium 默认激活但开发者不知情的问题

#### 🟠🟡 功能增强（4项）

- **P1 同目录关联文件推断**：改了 Controller 自动检查同目录 Service 是否在 diff 中，不在则标注 `⚠️ 建议一并审查`。零成本纯文件名匹配，非阻断仅提示
- **P11 实现细节文档**：补充临时目录结构（`.hermes/tmp/auto-fix/{timestamp}/`）、6 步文件写入流程、5 场景回滚策略
- **P0 框架不匹配提示**：检测到非 React/NestJS 框架时一行提示引导用户使用合适 Skill
- **术语约束范围收窄**：术语强制规范从"全文禁止混用"收窄为"仅约束机器可读字段"，业务描述允许自然中英混用

#### 📋 v7 Roadmap 新增

- **Fast Path 智能路径检测**：替代硬编码高风险路径名单，基于 import 频率和调用链分析自动识别

### 版本数据

- SKILL.md：1,549 → ~1,660 行
- P9 资损升级门槛：0.9 → 0.95
- P11 新增实现细节：+50 行

---

## v6.2.0 — AI 应用安全：LLM 专属防线

### 一句话

P2.7 按需激活：检测到 LLM SDK / 向量数据库 / RAG 框架依赖时，自动加载 6 条 AI 应用安全规则。

### 新增规则

- 🔴 LLM 降级兜底（try-catch + 预设话术）
- 🟡 完整四层降级链（初版可选）
- 🔴 LLM 输出安全渲染（格式校验 + XSS 清洗，原规则合并）
- 🟠 输出可信度（source/citations）
- 🔴 Prompt 注入防线（仅 diff 含 prompt 模板时触发）
- 🟠 Token 管理（topK/score/maxTokens）

### 版本数据

- SKILL.md：1,510 → 1,549 行
- P2 子模块：6 → 7 个
- 无 LLM 依赖项目零扫描开销

---

## v6.1.0 — UX 体验增强：双维度风险真正落地

### 一句话

从"双维度标签"升级为"双维度检测能力"。新增 6 条静态 UX 规则、AI 链路推演、资损升级通道、双板块报表。

### 四层增强

**静态规则**：P7 新增 7.6 通用交互体验，6 条跨场景 UX 规则（删除二次确认/提交防重/错误封装/空状态兜底/原生弹窗/输入提示），全局生效不受 P0.5 场景限制。

**AI 推演**：P9 新增结构化 UX 链路推演清单（操作闭环/异常兜底/边界状态 + 业务场景专属），传入 P0.5 场景矩阵辅助判定，每文件上限 3 条，按资损类 > 高频投诉 > 通用体验排序。新增 `issue_type` 字段（tech_risk / ux_risk），仅前端业务/全栈/高风险路径三档触发。

**跨端契约**：P4.4 同源扫描追加 UX 友好度检定，复用 dedup 不产生重复告警。

**报表重构**：按 `issue_type` 自动拆分为「技术故障」和「用户投诉风险」两个独立板块。UX 板块内按 experience_risk 分组排序。每条 UX 缺陷附带投诉场景说明。

**资损升级**：支付/退款/核销/删除四类场景 + 直接因果 + 置信度 ≥ 0.9 三要素门槛，可升级为 ux_derived_asset_loss（🔴Critical），保留在 UX 板块高亮展示。

**Fast Path**：前端工具档追加 3 条核心 P7 UX 规则（删除确认/防重/错误透传），a11y/i18n 仍跳过。

### 版本数据

- SKILL.md：1,418 → 1,510 行
- 静态 UX 规则：20 → 26 条
- P9 推演维度：新增 6 个 UX 推演维度
- 报表板块：1 → 2 个

---

## v6.0.0 — Diff-Only 架构升级：改什么，审什么

### 一句话

从「无论变更大小全库扫描」升级为「严格基于 `git add` 变更范围增量审查」。pre-commit 只审你改的文件，上下文不足就跳过+标注原因，CI `--full` 兜底。

### 三大核心升级

#### 1. 🔒 Diff-Only 原则全链路落地

- 全链路 15 个审查阶段全部改为仅读取 `git diff --cached` 暂存区变更
- 强制 `git show :<path>` 读取暂存快照，禁止读工作区文件（兼容 `git add -p`）
- 需要跨文件/全局上下文的校验（路由对齐、跨文件鉴权基线、全局状态一致性等）不再强行全库遍历，改为 **⏭️ 跳过 + 标注原因**，CI `--full` 兜底

#### 2. ⚡ Fast Path 六档自适应分流

变更类型预判，按风险权重六档分流：

```
纯非代码 < 前端样式 < 前端工具 < 后端工具 < 全栈变更 < 高风险路径
```

- 就高不就低（改了 CSS + Controller → 按 Controller 走全栈）
- 高风险路径最高优先级，命中后叠加 P2+P9
- CI `--full` 强制禁用分流

| 档位 | 改了什么 | 实际跑了什么 | 耗时 |
|------|---------|------------|------|
| 纯非代码 | README.md / .gitignore | P0→P1→P10 | < 1s |
| 前端样式 | .css / .scss | + 基础样式合规 | < 2s |
| 前端工具 | utils 通用组件 | + P2+P6 | < 5s |
| 后端工具 | 纯 .ts 工具函数 | + P2 | < 3s |
| 高风险路径 | auth/request/router 等 | 叠加 P2+P9 | ~10s |
| 全栈变更 | 业务代码 | P0~P11 完整 | ~15s |

#### 3. 🛡️ 鉴权删除检测补齐（前后端对称）

P4.6 新增两层分级，前后端规则 100% 对称：

| 级别 | 后端 | 前端 |
|------|------|------|
| 🔴 Critical | 行级删除 `@UseGuards` / `@Roles` | 路由级删除权限标记 / 请求拦截鉴权 |
| 🟠 High | — | 组件级删除权限指令 |
| 🟡 Warning | 文件最终无 Guard | 路由/组件最终无权限标记 |

新增/修改均检测。

### 配套升级

- **报表全透明**：新增状态列（✅完整 / ⚠️部分 / ⏭️跳过）+ 跳过原因列
- **CI 兜底提示**：有跳过项时显示，全部完整时隐藏
- **跳过项统计**：总览顶部显示检查项总数/完整数/跳过数
- **部分执行细化**：P4.6 拆分"已完成 X；未执行 Y，原因：Z"
- **高风险底层路径**：前后端默认名单（auth/request/router/permission/store），命中强制 P2+P9
- **非文本过滤**：后缀黑名单+内容兜底，二进制/图片/.lock 直接排除
- **路由弱提示**：P4.1 跳过时附加 🟡 "建议人工核对路由契约"
- **规则与实现分层**：两套模式复用同一套规则，本地和 CI 口径一致

### 分层防御

| 场景 | 模式 | 行为 | 定位 |
|------|------|------|------|
| 本地 pre-commit | Incremental（默认） | 仅审 diff，允许降级 | 开发体验优先 |
| CI 流水线 | `--full` | 全库强制，禁用分流 | 最后一道闸门 |

### 版本数据

- SKILL.md：1,278 行 → 1,418 行
- 审查阶段：15 个（P0 / P0.5 / P1~P11）
- Fast Path 档位：6 档
- 前后端规则对称度：100%

---

## v5.0.0 — Diff-Only 原则引入

### 核心改动

**Diff-Only 原则正式确立**：Incremental Mode 下所有阶段只读取 `git diff --cached` 中的文件，不遍历全库。

### 阶段改造

| 阶段 | 改前 | 改后 |
|------|------|------|
| P0.5 | 全库扫 Controller/Service | 仅解析 diff 文件推断场景 |
| P3 | 全库对比跨端一致性 | 仅审查 diff 文件 |
| P4.1 | 全库扫前后端路由 | diff 中两端同时存在才比对；缺一端 → ⏭️ 跳过 |
| P4.4 | 全库文案对比 | 仅 diff 两端同时存在才执行 |
| P4.5 | 全库扫状态管理 | 仅扫 diff 文件 |
| P4.6 | 全库扫 Controller | 分层：自检 ✅ + 跨文件对比 ⏭️ |
| P5 | 全库扫场景代码 | 仅扫 diff 中归属已激活场景的文件 |
| P5.2 | 永远执行 | diff 无 DB 操作 → ⏭️ 跳过 |
| P8 | 全库扫队列 | diff 无队列文件 → ⏭️ 跳过 |

### 报表升级

- 新增状态列：✅ 完整 / ⚠️ 部分 / ⏭️ 跳过
- 新增跳过原因列
- 状态枚举标准化
- CI 兜底提示

### 两层防御

```
本地 pre-commit → Incremental：快，diff 驱动，允许降级
CI 流水线     → --full：全库强制，不允许降级
```

### 版本数据

- SKILL.md：~1,200 行 → 1,278 行
- 审查阶段：15 个

---

## v4.0.0 — 初始版本

### 核心架构

- 15 阶段自动化审查流水线（P0~P11 + P4.6）
- 7 大业务场景按需激活（交易/资源抢占/线下履约/权益/信息交互/异步长任务/消息队列）
- 四端覆盖：Web / H5 / App / 小程序
- 双分支审查：🔒 Tech Risk + 🧑 UX Risk

### 安全体系

- P2 拆 6 个子模块：硬编码/注入/NestJS 专项/React 专项/依赖 CVE/日志脱敏/环境配置
- P4.6 多租户 & 权限鉴权基线
- AI Reviewer 子智能体 + 置信度过滤
- P11 按需修复 + 黑白名单 + 编译校验 + 重验证

### 报表

- 双标签体系（技术严重度 + 体验风险度）
- fix_priority 自动赋值
- commit 阻断阈值标准化

### 版本数据

- SKILL.md：~1,217 行
- 审查阶段：15 个
